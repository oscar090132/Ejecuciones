@model IEnumerable<Ejecuciones.Models.EstadoSolicitud>

@{
    ViewData["Title"] = "Estado Solicitud";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Estado Solicitud</h1>

<p>
    <a asp-action="Create">Nuevo Estado</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstadoSolicitud)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstadoSolicitud)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstadoSolicitudId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.EstadoSolicitudId">Detalle</a> |
                <a asp-action="Delete" asp-route-id="@item.EstadoSolicitudId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
