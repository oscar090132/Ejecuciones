@model IEnumerable<Ejecuciones.Models.EstadoSolicitud>

@{
    ViewData["Title"] = "Ordenar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstadoSolicitud)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstadoSolicitud)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstadoSolicitudId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstadoSolicitudId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstadoSolicitudId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
