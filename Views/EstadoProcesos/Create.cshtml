@model Ejecuciones.Models.EstadoProceso

@{
    ViewData["Title"] = "Estado Proceso";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Crear</h1>

<h3>Estado Proceso</h3>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NombreEstadoProceso" class="control-label"></label>
                <input asp-for="NombreEstadoProceso" class="form-control" />
                <span asp-validation-for="NombreEstadoProceso" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver a la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
