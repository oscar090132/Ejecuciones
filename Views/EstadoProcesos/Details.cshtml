@model Ejecuciones.Models.EstadoProceso

@{
    ViewData["Title"] = "Estado Proceso";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Detalle</h1>

<h3>Estado Proceso</h3>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NombreEstadoProceso)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NombreEstadoProceso)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.EstadoProcesoId">Editar</a> |
    <a asp-action="Index">Volver a la lista</a>
</div>
