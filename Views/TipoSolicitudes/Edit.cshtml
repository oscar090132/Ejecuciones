@model Ejecuciones.Models.TipoSolicitud

@{
    ViewData["Title"] = "Tipo Solicitud";
}

<h1>Editar</h1>

<h3>Tipo Solicitud</h3>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TipoSolicitudId" />
            <div class="form-group">
                <label asp-for="NombreSolicitud" class="control-label"></label>
                <input asp-for="NombreSolicitud" class="form-control" />
                <span asp-validation-for="NombreSolicitud" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver a la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
